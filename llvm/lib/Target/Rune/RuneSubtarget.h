#ifndef LLVM_LIB_TARGET_RUNE_RUNESUBTARGET_H
#define LLVM_LIB_TARGET_RUNE_RUNESUBTARGET_H

#include "Rune.h"
#include "RuneFrameLowering.h"
#include "RuneISelLowering.h"
#include "RuneInstrInfo.h"
#include "RuneRegisterInfo.h"
#include "llvm/CodeGen/SelectionDAGTargetInfo.h"
#include "llvm/CodeGen/TargetSubtargetInfo.h"

#define GET_SUBTARGETINFO_HEADER
#include "RuneGenSubtargetInfo.inc"

namespace llvm {

class RuneSubtarget : public RuneGenSubtargetInfo {
  RuneTargetLowering TLInfo;
  RuneFrameLowering FrameLowering;
  RuneRegisterInfo RegInfo;
  RuneInstrInfo InstrInfo;
  SelectionDAGTargetInfo TSInfo;

public:
  RuneSubtarget(const Triple &TT, const std::string &CPU, const std::string &FS,
               const TargetMachine &TM);

  /// ParseSubtargetFeatures - Parses features string setting specified
  /// subtarget options.  Definition of function is auto generated by tblgen.
  void ParseSubtargetFeatures(StringRef CPU, StringRef TuneCPU, StringRef FS);

  const RuneTargetLowering *getTargetLowering() const override {
    TICK_CYAN()
    return &TLInfo;
  }
  const RuneFrameLowering *getFrameLowering() const override {
    TICK_CYAN()
    return &FrameLowering;
  }
  const RuneRegisterInfo *getRegisterInfo() const override {
    TICK_CYAN()
    return &RegInfo;
  }
  const RuneInstrInfo *getInstrInfo() const override { return &InstrInfo; }
  const SelectionDAGTargetInfo *getSelectionDAGInfo() const override {
    TICK_CYAN()
    return &TSInfo;
  }
};

} // end namespace llvm

#endif // LLVM_LIB_TARGET_RUNE_RUNESUBTARGET_H